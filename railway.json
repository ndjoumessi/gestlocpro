{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build && npm --prefix server ci && npm --prefix server exec prisma generate && npm --prefix server run build"
  },
  "deploy": {
    "startCommand": "npm --prefix server run start",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
